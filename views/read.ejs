<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="shortcut icon" href="/images/add_icon_129463 (1).ico" type="image/x-icon">
</head>

<body>
    <div class="min-h-full w-full bg-zinc-900 p-10 text-white">
        <form action="">
            <div class="nav text-xl text-blue-500 text-3xl px-5 py-3 mb-5">
                <a href="/">Back to home.⬅️</a>
            </div>

            <h1 class="text-3xl px-5 py-3 mb-5">ALL Users</h1>
            <div class="users flex gap-2 flex-wrap">
                <% users.forEach(function(users){ %>
                    <div class="user w-72 p-4 bg-zinc-800 rounded-xl">
                        <div class="w-full h-72 rounded-xl bg-zinc-900 overflow-hidden">
                            <img class="w-full h-full object-top object-cover" src="<%= users.image %>" alt="">

                        </div>
                        <div class="mt-5">
                            <h3 class="text-xl"> <%= users.name %></h3>
                            <h4 class="text-zinc-300"><%= users.email %></h4>
                        </div>
                        <div class="flex mt-5 gap-4 justify-center items-center">
                            <a class="text-zinc-200 edit">Edit User</a>
                            <a class="text-red-400 delete" href="/delete/<%=users._id%>">Delete User</a>
                        </div>
                    </div>
                <% }) %>
                
                
            </div>
        </form>
    </div>
</body>

</html>